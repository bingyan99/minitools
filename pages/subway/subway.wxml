
<view class="map-container">
  <view class="list-item" wx:for="{{ cityList }}" wx:key="key" wx:for-item="data" data-adcode="{{ data.adcode }}" bindtap="gotoSubway">
    {{ data.cityname }}
  </view>
</view>

